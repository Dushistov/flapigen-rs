pub enum TestEnum {
    A,
    B,
}

foreign_enum!(enum TestEnum {
    A = TestEnum::A,
    B = TestEnum::B,
});

pub struct TestClass {
}

impl TestClass {
    pub fn print_hello() {
        println!("Hello from rust");
    }
}

foreigner_class!(class TestClass {
    static_method TestClass::print_hello();
});
